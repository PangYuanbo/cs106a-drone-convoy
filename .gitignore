# macOS
.DS_Store

# Vercel
.vercel/

# Editor noise
.vscode/
.idea/
*.swp
*.swo

# Project source — lives in its own repo (Tyler6666666/106a_final_project).
# We only ship the rendered site + the demo videos referenced by index.html.
106a_final_project/

# Logs
*.log
ogre.log

# Node / Next.js — this repo ships static index.html only; build artefacts are local-only
node_modules/
.next/
out/
tsconfig.tsbuildinfo

# Env files — never commit (.env.local has Vercel OIDC tokens)
.env
.env.local
.env.*.local

# Orphaned Next.js scaffolding kept on disk but not tracked
app/
components/
data/
public/
hero-image-examples-mars-explorer.png.webp

CS_106A_Final_Project.pdf
section-gap-change.md